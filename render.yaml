services:
  - type: web
    name: al-insaf-ngo
    env: python
    buildCommand: pip install -r requirements.txt && python create_db.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: FLASK_ENV
        value: production
